name: Native pixel asset validation
on:
  push:
    branches: [main, 'codex/native-pixel-assets']
    paths: ['assets/**', 'native-*', 'scripts/*native*', 'scripts/build-static.cjs', 'tests/*asset*', '.github/workflows/native-assets.yml']
  pull_request:
    paths: ['assets/**', 'native-*', 'scripts/*native*', 'scripts/build-static.cjs', 'tests/*asset*', '.github/workflows/native-assets.yml']
permissions:
  contents: read
jobs:
  native-assets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm test
      - run: npm run build
      - uses: actions/upload-artifact@v4
        with:
          name: native-pixel-assets
          path: |
            assets/native/
            native-sprites.js
            native-asset-review.html
            scripts/build-native-assets.cjs
            scripts/build-native-review.cjs
          retention-days: 14
