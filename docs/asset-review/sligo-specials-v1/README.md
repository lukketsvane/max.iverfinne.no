# Sligo specials (owner's source, not yet used)

The owner's Max Level Studio export with more material for Sligo ("specials and so on"), kept as
delivered for the next step: `level.json` (46 placed objects, 351 assets, Max x/rise coordinates,
origin 0, baseline 0) and `assets/` (the transparent pieces). `studio-README.txt` is the export's own
note. Nothing here is loaded by the game yet.
